# SDK Structure

```typescript
const sdk = new FutarchySDK({ network: 'devnet' });

// ============================================================================
// SERVICES
// ============================================================================

sdk.dao  // DAO operations
  .getInfo(daoId)
  .getConfig(daoId)
  .getProposals(daoId)
  .getAll(factoryPackageId)
  .getByCreator(factoryPackageId, creator)
  .getEvent(factoryPackageId, accountId)
  .getManagedObjects(daoId)
  .addManagedObject(config)
  .removeManagedObject(config)
  .borrowManagedObject(config)
  .hasManagedObject(daoId, name)
  .depositObject(config)
  .withdrawObject(config)
  .configureDeposits(config)
  .addToWhitelist(config)
  .removeFromWhitelist(config)
  .getObjectTrackerState(daoId)
  .cleanupExpiredIntents(daoAccountId, maxToClean, clockId)
  .checkMaintenanceNeeded(daoAccountId, clockId)
  .create(config)
  .createWithSpecs(config, initSpecs)
  .oracle
    .claimGrant(grantId, clockId)
    .getGrants(daoId)
  .vault
    .depositApproved(config)
    .depositSui(config)
    .claimStream(config)
    .transferStream(config)
    .getBalance(daoId, vaultName, coinType)
    .getStream(streamId)
    .getClaimableAmount(streamId)
    .listStreamsForBeneficiary(beneficiary)
    .isCoinTypeApproved(daoId, vaultName, coinType)

sdk.launchpad  // Launchpad operations
  .createRaise(config)
  .startRaise(config)
  .contribute(config)
  .complete(config)
  .claim(raiseId, assetType, stableType, clockId)
  .executeActions(config)
  .getRaise(raiseId)
  .getContributions(raiseId)
  .getContributionsBy(contributor)
  .getContributionBy(raiseId, contributor)
  .isSettled(raiseId)
  .getState(raiseId)
  .getTotalRaised(raiseId)
  .getTokenClaims(raiseId)
  .getRefundClaims(raiseId)
  .getStartTime(raiseId)
  .getDeadline(raiseId)
  .getDescription(raiseId)
  .getFinalAmount(raiseId)
  .getAllowedCaps(raiseId)
  .getCapSums(raiseId)
  .getVerificationLevel(raiseId)
  .getAttestationUrl(raiseId)
  .getAdminReviewText(raiseId)
  .getAll()
  .getByCreator(creator)

sdk.proposal  // Proposal operations
  .create(config)
  .startProposal(config)
  .addActions(config)
  .finalizeAndExecute(config)
  .redeemTokens(proposalId, escrowId, assetType, stableType, conditionalCoinId, conditionalCoinType, outcomeIndex, isAsset, recipient, clockId)
  .advanceToReview(config)
  .advanceToTrading(config)
  .spotSwap(config)
  .cleanUpExpiredIntents(daoAccountId, maxToClean, clockId)
  .getInfo(proposalId)
  .getMarket(proposalId)
  .getAll()
  .getByDao(daoId)
  .sponsorship
    .sponsor(config)
    .sponsorToZero(config)
    .canSponsor(proposalId, daoId, quotaRegistryId, potentialSponsor, assetType, stableType, clock)
  .trade
    .stableForAsset(config)
    .assetForStable(config)
    .getQuote(proposalId, outcomeIndex, amountIn, direction)
    .getPrice(proposalId, outcomeIndex)
  .twap
    .getCurrentPrice(proposalId, outcomeIndex)
    .getObservations(proposalId)
    .recordObservation(marketStateId, clockId)
  .escrow
    .hasEscrowReceipt(proposalId, outcomeIndex, assetType, stableType)
    .getBalance(escrowId, assetType)
    .isEmpty(escrowId, assetType)

sdk.market  // Market operations
  .swapAssetForStable(config)
  .swapStableForAsset(config)
  .swapSuiForAsset(config)
  .getQuote(config)
  .getPrice(daoId)
  .swapAssetForStableDirect(config)
  .swapStableForAssetDirect(config)
  .getTotalFeeBps(poolId)
  .getPriceByPoolId(poolId)
  .getLpTokenSupply(poolId)
  .getLpBalance(daoId, owner)
  .pool
    .addLiquidity(config)
    .removeLiquidity(config)
    .get(daoId)
    .getReserves(poolId)
    .getLpBalance(daoId, owner)
    .getTotalFeeBps(poolId)
    .getLpTokenSupply(poolId)

sdk.admin  // Admin operations (requires admin capabilities)
  .factory  // Factory admin operations (requires FactoryOwnerCap)
    .togglePause(factoryOwnerCapId)
    .disablePermanently(factoryOwnerCapId, clock)
    .addAllowedStableType(stableCoinType, factoryOwnerCapId, clock)
    .removeAllowedStableType(stableCoinType, factoryOwnerCapId, clock)
    .updateLaunchpadFees(config, factoryOwnerCapId, clock)
    .burnFactoryOwnerCap(factoryOwnerCapId)
    .borrowCoinMetadata(accountId, coinType)
    .getDaoCount()
    .isPaused()
    .isPermanentlyDisabled()
    .isStableTypeAllowed(stableType)
    .getAllowedStableTypes()
    .getLaunchpadBidFee()
    .getLaunchpadCrankerReward()
    .getLaunchpadSettlementReward()
  .verification  // Factory validator operations (requires ValidatorAdminCap)
    .approveVerification(config, validatorCapId, clock)
    .rejectVerification(config, validatorCapId, clock)
    .setDaoScore(config, validatorCapId, clock)
  .packageRegistry  // Package registry admin operations (requires PackageAdminCap)
    .pauseAccountCreation(packageAdminCapId)
    .unpauseAccountCreation(packageAdminCapId)
    .addPackage(config)
    .removePackage(packageName)
    .updatePackageVersion(config)
    .updatePackageMetadata(config)
  .feeManager  // Fee manager operations (requires FeeAdminCap)
    .addCoinFeeConfig(config, adminCapId, clock)
    .updateDaoCreationFee(newFee, adminCapId, clock)
    .updateProposalCreationFee(newFee, adminCapId, clock)
    .updateLaunchpadCreationFee(newFee, adminCapId, clock)
    .withdrawStableFees(params, adminCapId)
    .withdrawAssetFees(params, adminCapId)
    .withdrawAllSuiFees(adminCapId, recipient)
    .getDaoCreationFee()
    .getProposalCreationFee()
    .getLaunchpadCreationFee()
    .getSuiBalance()

sdk.intents  // Intent-related functionality including staging, execution, and queries. Primarily intended for internal use; users are generally discouraged from direct access.
  .executor  // Intent executor for executing staged actions
    .execute(config)
  .composer  // Transaction composer for building complex PTBs
    .new()
  .oracleQueries  // Oracle query operations
    .getTotalAmount(grantId)
    .isCanceled(grantId)
    .getDescription(grantId)
    .getTierCount(grantId)
    .getAllGrantIds(accountId)
  .vaultQueries  // Vault query operations
    .getStream(streamId)
    .getClaimableAmount(streamId)
    .listStreamsForBeneficiary(beneficiary)
  .staging
    .stream
      .addCreateStream(tx, builder, actionsPackageId, config)
      .calculatePerIteration(totalAmount, iterations)
      .startAfter(offsetMs)
    .currency
      .addReturnTreasuryCap(tx, builder, actionsPackageId, config)
      .addReturnMetadata(tx, builder, actionsPackageId, config)
      .addLockTreasuryCap(tx, builder, actionsPackageId)
      .addStoreMetadata(tx, builder, actionsPackageId)
      .addMint(tx, builder, actionsPackageId, amount)
      .addBurn(tx, builder, actionsPackageId, amount)
      .addDisable(tx, builder, actionsPackageId, config)
      .addUpdate(tx, builder, actionsPackageId, config)
    .vault
      .addDeposit(tx, builder, actionsPackageId, config)
      .addSpend(tx, builder, actionsPackageId, config)
      .addApproveCoinType(tx, builder, actionsPackageId, config)
      .addRemoveApprovedCoinType(tx, builder, actionsPackageId, config)
      .addCancelStream(tx, builder, actionsPackageId, config)
    .transfer
      .addWithdrawAndTransfer(tx, builder, actionsPackageId, config)
      .addTransferObject(tx, builder, actionsPackageId, config)
      .addTransferToSender(tx, builder, actionsPackageId, config)
    .memo
      .addEmitMemo(tx, builder, actionsPackageId, config)
    .accessControl
      .addBorrow(tx, builder, actionsPackageId)
      .addReturn(tx, builder, actionsPackageId)
    .packageUpgrade
      .addUpgrade(tx, builder, actionsPackageId, config)
      .addCommit(tx, builder, actionsPackageId, config)
      .addRestrict(tx, builder, actionsPackageId, config)
      .addCreateCommitCap(tx, builder, actionsPackageId, config)
    .config
      .addUpdateTradingParams(tx, builder, futarchyActionsPackageId, config)
      .addUpdateTwapConfig(tx, builder, futarchyActionsPackageId, config)
      .createThresholdOption(tx, typesPackageId, value)
      .addUpdateMetadataTable(tx, builder, futarchyActionsPackageId, config)
      .addSetProposalsEnabled(tx, builder, futarchyActionsPackageId, config)
      .addTerminateDao(tx, builder, futarchyActionsPackageId)
      .addUpdateName(tx, builder, futarchyActionsPackageId, config)
      .addUpdateMetadata(tx, builder, futarchyActionsPackageId, config)
      .addUpdateGovernance(tx, builder, futarchyActionsPackageId, config)
      .addUpdateSponsorshipConfig(tx, builder, futarchyActionsPackageId, config)
      .createSponsoredThresholdOption(tx, typesPackageId, value)
      .addUpdateConditionalMetadata(tx, builder, futarchyActionsPackageId, config)
      .addBatchConfig(tx, builder, futarchyActionsPackageId, config)
    .quota
      .addSetQuotas(tx, builder, futarchyActionsPackageId, config)
    .liquidityInit
      .addCreatePoolWithMint(tx, builder, futarchyActionsPackageId, config)
      .addCreatePoolFromVault(tx, builder, futarchyActionsPackageId, config)
      .addLiquidityToPool(tx, builder, futarchyActionsPackageId, config)
      .calculatePrice(assetAmount, stableAmount)
      .calculateAssetForPrice(stableAmount, targetPrice)
    .liquidityMarkers
      .addLiquidityMarker(tx, futarchyActionsPackageId)
      .removeLiquidityMarker(tx, futarchyActionsPackageId)
      .swapMarker(tx, futarchyActionsPackageId)
    .liquidityConstructors
      .newAddLiquidityAction(tx, config)
      .newRemoveLiquidityAction(tx, config)
      .newSwapAction(tx, config)
    .liquidityExecutors
      .doAddLiquidity(tx, config, executable, versionWitness, intentWitness)
      .doRemoveLiquidity(tx, config, executable, versionWitness, intentWitness)
      .doSwap(tx, config, executable, versionWitness, intentWitness)
    .liquidityGetters
      .getPoolId(tx, futarchyActionsPackageId, assetType, stableType, lpType, action)
      .getAssetAmount(tx, futarchyActionsPackageId, assetType, stableType, lpType, action)
      .getStableAmount(tx, futarchyActionsPackageId, assetType, stableType, lpType, action)
      .getMinLpAmount(tx, futarchyActionsPackageId, assetType, stableType, lpType, action)
    .dissolution
      .addCreateDissolutionCapability(tx, builder, futarchyActionsPackageId, config)
    .packageRegistry
      .addAddPackage(tx, builder, governanceActionsPackageId, config)
      .addRemovePackage(tx, builder, governanceActionsPackageId, config)
      .addUpdatePackageVersion(tx, builder, governanceActionsPackageId, config)
      .addUpdatePackageMetadata(tx, builder, governanceActionsPackageId, config)
      .addPauseAccountCreation(tx, builder, governanceActionsPackageId)
      .addUnpauseAccountCreation(tx, builder, governanceActionsPackageId)
    .protocolAdmin
      .addSetFactoryPaused(tx, builder, governanceActionsPackageId, config)
      .addDisableFactoryPermanently(tx, builder, governanceActionsPackageId)
      .addAddStableType(tx, builder, governanceActionsPackageId, config)
      .addRemoveStableType(tx, builder, governanceActionsPackageId, config)
      .addUpdateDaoCreationFee(tx, builder, governanceActionsPackageId, config)
      .addUpdateProposalFee(tx, builder, governanceActionsPackageId, config)
      .addUpdateVerificationFee(tx, builder, governanceActionsPackageId, config)
      .addAddVerificationLevel(tx, builder, governanceActionsPackageId, config)
      .addRemoveVerificationLevel(tx, builder, governanceActionsPackageId, config)
      .addWithdrawFeesToTreasury(tx, builder, governanceActionsPackageId, config)
      .addAddCoinFeeConfig(tx, builder, governanceActionsPackageId, config)
      .addUpdateCoinCreationFee(tx, builder, governanceActionsPackageId, config)
      .addUpdateCoinProposalFee(tx, builder, governanceActionsPackageId, config)
      .addApplyPendingCoinFees(tx, builder, governanceActionsPackageId, config)
    .oracle
      .addCreateOracleGrant(tx, builder, oracleActionsPackageId, config)
      .addCancelGrant(tx, builder, oracleActionsPackageId, config)
  .execution
    .vault
      .vaultDeposit(tx, accountActionsPackageId)
      .vaultSpend(tx, accountActionsPackageId)
      .vaultApproveCoinType(tx, accountActionsPackageId)
      .vaultRemoveApprovedCoinType(tx, accountActionsPackageId)
      .cancelStream(tx, accountActionsPackageId)
      .createStream(tx, accountActionsPackageId)
      .open(tx, config, auth, account, registry, name)
      .deposit(tx, config, auth, account, registry, name, coin)
      .depositApproved(tx, config, account, registry, name, coin)
      .spend(tx, config, auth, account, registry, name, amount)
      .approveCoinType(tx, config, auth, account, registry, name)
      .removeApprovedCoinType(tx, config, auth, account, registry, name)
      .close(tx, config, auth, account, registry, name)
      .createStreamAuth(tx, config, auth, account, registry, vaultName, beneficiary, amountPerIteration, startTime, iterationsTotal, iterationPeriodMs, cliffTime, claimWindowMs, maxPerWithdrawal, isTransferable, isCancellable, clock)
      .cancelStreamAuth(tx, config, auth, account, registry, vaultName, streamId, clock)
      .withdrawFromStream(tx, config, account, registry, vaultName, streamId, amount, clock)
      .balance(tx, config, account, registry, name)
      .hasVault(tx, config, account, name)
      .borrowVault(tx, config, account, registry, name)
      .size(tx, config, vault)
      .coinTypeExists(tx, config, vault)
      .coinTypeValue(tx, config, vault)
      .isCoinTypeApproved(tx, config, account, registry, name)
      .calculateClaimable(tx, config, account, registry, vaultName, streamId, clock)
      .streamInfo(tx, config, account, registry, vaultName, streamId)
      .hasStream(tx, config, account, registry, vaultName, streamId)
      .doDeposit(tx, config, executable, account, registry, coin, versionWitness, intentWitness)
      .doSpend(tx, config, executable, account, registry, versionWitness, intentWitness)
      .doCancelStream(tx, config, executable, account, registry, vaultName, clock, versionWitness, intentWitness)
      .doApproveCoinType(tx, config, executable, account, registry, versionWitness, intentWitness)
      .doRemoveApprovedCoinType(tx, config, executable, account, registry, versionWitness, intentWitness)
      .doInitCreateStream(tx, config, executable, account, registry, clock, versionWitness, intentWitness)
      .doInitDeposit(tx, config, executable, account, registry, versionWitness, intentWitness)
      .doInitWithdrawAndTransfer(tx, config, executable, account, registry, versionWitness, intentWitness)
      .deleteDeposit(tx, config, expired)
      .deleteSpend(tx, config, expired)
      .deleteCancelStream(tx, config, expired)
      .deleteApproveCoinType(tx, config, expired)
      .deleteRemoveApprovedCoinType(tx, config, expired)
      .deleteToggleStreamPause(tx, config, expired)
      .deleteToggleStreamFreeze(tx, config, expired)
      .destroyDepositAction(tx, config, action)
      .destroySpendAction(tx, config, action)
      .destroyCancelStreamAction(tx, config, action)
      .destroyApproveCoinTypeAction(tx, config, action)
      .destroyRemoveApprovedCoinTypeAction(tx, config, action)
      .defaultVaultName(tx, config)
      .streamClaimableNow(tx, config, vault, streamId, clock)
      .streamNextVestTime(tx, config, vault, streamId, clock)
    .currency
      .currencyLockCap(tx, accountActionsPackageId)
      .currencyDisable(tx, accountActionsPackageId)
      .currencyMint(tx, accountActionsPackageId)
      .currencyBurn(tx, accountActionsPackageId)
      .currencyUpdate(tx, accountActionsPackageId)
      .treasuryCapKey(tx, config)
      .coinMetadataKey(tx, config)
      .currencyRulesKey(tx, config)
      .newCurrencyRules(tx, config, maxSupply, canMint, canBurn, canUpdateSymbol, canUpdateName, canUpdateDescription, canUpdateIcon)
      .lockCap(tx, config, auth, account, registry, treasuryCap, maxSupply)
      .borrowTreasuryCapMut(tx, config, account, registry)
      .borrowRules(tx, config, account, registry)
      .readCoinMetadata(tx, config, metadata)
      .publicBurn(tx, config, account, registry, coin)
      .hasCap(tx, config, account)
      .coinTypeSupply(tx, config, account, registry)
      .maxSupply(tx, config, lock)
      .totalMinted(tx, config, lock)
      .totalBurned(tx, config, lock)
      .canMint(tx, config, lock)
      .canBurn(tx, config, lock)
      .canUpdateSymbol(tx, config, lock)
      .canUpdateName(tx, config, lock)
      .canUpdateDescription(tx, config, lock)
      .canUpdateIcon(tx, config, lock)
      .doDisable(tx, config, executable, account, registry, versionWitness, intentWitness)
      .doUpdate(tx, config, executable, account, registry, metadata, versionWitness, intentWitness)
      .doMint(tx, config, executable, account, registry, versionWitness, intentWitness)
      .doBurn(tx, config, executable, account, registry, coin, versionWitness, intentWitness)
      .doInitBurn(tx, config, executable, account, registry, versionWitness, intentWitness)
      .deleteDisable(tx, config, expired)
      .deleteUpdate(tx, config, expired)
      .deleteMint(tx, config, expired)
      .deleteBurn(tx, config, expired)
      .destroyMintAction(tx, config, action)
      .destroyBurnAction(tx, config, action)
      .doInitRemoveTreasuryCap(tx, config, executable, account, registry, versionWitness, intentWitness)
      .doInitRemoveMetadata(tx, config, executable, account, registry, key, versionWitness, intentWitness)
    .transfer
      .transferObject(tx, accountActionsPackageId)
      .transferToSender(tx, accountActionsPackageId)
      .doTransfer(tx, config, executable, intentWitness, object)
      .doTransferUnshared(tx, config, object, recipient)
      .doTransferToSender(tx, config, executable, intentWitness, object)
      .doInitTransfer(tx, config, executable, intentWitness)
      .doInitTransferToSender(tx, config, executable, intentWitness)
      .deleteTransfer(tx, config, expired)
      .deleteTransferToSender(tx, config, expired)
      .destroyTransferAction(tx, config, action)
      .destroyTransferToSenderAction(tx, config, action)
    .memo
      .memo(tx, accountActionsPackageId)
      .doEmitMemo(tx, config, executable, intentWitness, clock)
      .deleteMemo(tx, config, expired)
      .destroyEmitMemoAction(tx, config, action)
    .accessControl
      .accessControlStore(tx, config)
      .accessControlBorrow(tx, config)
      .accessControlReturn(tx, config)
      .lockCap(tx, config, auth, account, registry, cap)
      .doLockCapUnshared(tx, config, account, registry, cap)
      .hasLock(tx, config, account)
      .destroyBorrowAction(tx, config, action)
      .destroyReturnAction(tx, config, action)
      .doBorrow(tx, config, executable, account, registry, versionWitness, intentWitness)
      .deleteBorrow(tx, config, expired)
      .doReturn(tx, config, executable, account, registry, cap, versionWitness, intentWitness)
      .deleteReturn(tx, config, expired)
    .packageUpgrade
      .packageUpgrade(tx, accountActionsPackageId)
      .packageCommit(tx, accountActionsPackageId)
      .packageRestrict(tx, accountActionsPackageId)
      .packageCreateCommitCap(tx, accountActionsPackageId)
      .lockCap(tx, accountActionsPackageId, auth, account, registry, cap, name, delayMs, reclaimDelayMs)
      .lockCommitCap(tx, accountActionsPackageId, auth, account, registry, packageName)
      .createAndTransferCommitCap(tx, config, auth, account, registry, packageName, recipient)
      .borrowCommitCap(tx, config, account, registry, packageName, versionWitness)
      .returnCommitCap(tx, config, account, registry, commitCap, versionWitness)
      .transferCommitCap(tx, accountActionsPackageId, cap, recipient)
      .destroyCommitCap(tx, config, cap, account, registry)
      .hasCap(tx, accountActionsPackageId, account, name)
      .hasCommitCap(tx, accountActionsPackageId, account, packageName)
      .commitCapPackageName(tx, accountActionsPackageId, cap)
      .commitCapValidNonce(tx, accountActionsPackageId, cap)
      .getCapPackage(tx, accountActionsPackageId, account, registry, name)
      .getCapVersion(tx, accountActionsPackageId, account, registry, name)
      .getCapPolicy(tx, accountActionsPackageId, account, registry, name)
      .getTimeDelay(tx, accountActionsPackageId, account, registry, name)
      .getPackagesInfo(tx, accountActionsPackageId, account, registry)
      .isPackageManaged(tx, accountActionsPackageId, account, registry, packageAddr)
      .getPackageAddr(tx, accountActionsPackageId, account, registry, packageName)
      .getPackageName(tx, accountActionsPackageId, account, registry, packageAddr)
      .getCurrentCommitNonce(tx, accountActionsPackageId, account, registry, packageName)
      .requestReclaimCommitCap(tx, config, auth, account, registry, packageName, clock)
      .clearReclaimRequest(tx, config, auth, account, registry, packageName, clock)
      .hasReclaimRequest(tx, accountActionsPackageId, account, registry, packageName)
      .getReclaimAvailableTime(tx, accountActionsPackageId, account, registry, packageName)
      .proposeUpgradeDigest(tx, accountActionsPackageId, auth, account, registry, packageName, digest, executionTimeMs, clock)
      .approveUpgradeProposal(tx, config, auth, account, registry, packageName, digest, clock)
      .hasUpgradeProposal(tx, accountActionsPackageId, account, packageName, digest)
      .isUpgradeApproved(tx, accountActionsPackageId, account, registry, packageName, digest)
      .getUpgradeProposal(tx, accountActionsPackageId, account, registry, packageName, digest)
      .executeApprovedUpgradeDaoOnly(tx, config, account, registry, packageName, digest, clock, versionWitness)
      .completeApprovedUpgradeDaoOnly(tx, config, account, registry, packageName, digest, receipt, versionWitness)
      .executeApprovedUpgradeWithCap(tx, config, account, registry, packageName, digest, commitCap, clock, versionWitness)
      .completeApprovedUpgradeWithCap(tx, config, account, registry, packageName, digest, receipt, commitCap, versionWitness)
      .doUpgrade(tx, config, executable, account, registry, clock, versionWitness, intentWitness)
      .doCommitDaoOnly(tx, config, executable, account, registry, receipt, clock, versionWitness, intentWitness)
      .doCommitWithCap(tx, config, executable, account, registry, receipt, commitCap, versionWitness, intentWitness)
      .doRestrict(tx, config, executable, account, registry, versionWitness, intentWitness)
      .doCreateCommitCap(tx, config, executable, account, registry, versionWitness, intentWitness)
      .deleteUpgrade(tx, accountActionsPackageId, expired)
      .deleteCommit(tx, accountActionsPackageId, expired)
      .deleteRestrict(tx, accountActionsPackageId, expired)
      .deleteCreateCommitCap(tx, accountActionsPackageId, expired)
      .destroyUpgradeAction(tx, accountActionsPackageId, action)
      .destroyCommitAction(tx, accountActionsPackageId, action)
      .destroyRestrictAction(tx, accountActionsPackageId, action)
      .destroyCreateCommitCapAction(tx, accountActionsPackageId, action)
    .packageUpgradeAuditable
      .proposeAuditableUpgrade(tx, config, auth, account, registry, packageName, bytecodeDigest, auditMetadata, executionTimeMs, clock)
      .addVerifierAttestation(tx, config, auth, account, registry, packageName, bytecodeDigest, signature, clock)
      .getAuditMetadata(tx, config, account, registry, packageName, bytecodeDigest)
      .newAuditMetadata(tx, config, sourceCodeHash, moveTomlHash, compilerVersion, buildTimestampMs, dependenciesHash, gitCommitHash, githubReleaseTag, auditReportUrl)
    .daoConfig
      .setProposalsEnabledMarker(tx, futarchyActionsPackageId)
      .terminateDaoMarker(tx, futarchyActionsPackageId)
      .updateNameMarker(tx, futarchyActionsPackageId)
      .tradingParamsUpdateMarker(tx, futarchyActionsPackageId)
      .metadataUpdateMarker(tx, futarchyActionsPackageId)
      .twapConfigUpdateMarker(tx, futarchyActionsPackageId)
      .governanceUpdateMarker(tx, futarchyActionsPackageId)
      .sponsorshipConfigUpdateMarker(tx, futarchyActionsPackageId)
      .newSetProposalsEnabled(tx, config)
      .newUpdateName(tx, config)
      .newTradingParamsUpdate(tx, config)
      .newMetadataUpdate(tx, config)
      .newTwapConfigUpdate(tx, config)
      .newGovernanceUpdate(tx, config)
      .newSponsorshipConfigUpdate(tx, config)
      .newMetadataTableUpdate(tx, config)
      .doSetProposalsEnabled(tx, config, executable, versionWitness, intentWitness)
      .doTerminateDao(tx, config, executable, versionWitness, intentWitness)
      .doUpdateName(tx, config, executable, versionWitness, intentWitness)
      .doUpdateTradingParams(tx, config, executable, versionWitness, intentWitness)
      .doUpdateMetadata(tx, config, executable, versionWitness, intentWitness)
      .doUpdateTwapConfig(tx, config, executable, versionWitness, intentWitness)
      .doUpdateGovernance(tx, config, executable, versionWitness, intentWitness)
      .doUpdateSponsorshipConfig(tx, config, executable, versionWitness, intentWitness)
      .getProposalsEnabled(tx, futarchyActionsPackageId, action)
      .getNewName(tx, futarchyActionsPackageId, action)
      .getTradingParamsFields(tx, futarchyActionsPackageId, action)
      .getMetadataFields(tx, futarchyActionsPackageId, action)
      .getTwapConfigFields(tx, futarchyActionsPackageId, action)
      .getGovernanceFields(tx, futarchyActionsPackageId, action)
      .getMetadataTableFields(tx, futarchyActionsPackageId, action)
      .destroySetProposalsEnabled(tx, futarchyActionsPackageId, action)
      .destroyUpdateName(tx, futarchyActionsPackageId, action)
      .destroyTradingParamsUpdate(tx, futarchyActionsPackageId, action)
      .destroyMetadataUpdate(tx, futarchyActionsPackageId, action)
      .destroyTwapConfigUpdate(tx, futarchyActionsPackageId, action)
      .destroyGovernanceUpdate(tx, futarchyActionsPackageId, action)
      .destroyMetadataTableUpdate(tx, futarchyActionsPackageId, action)
      .destroySponsorshipConfigUpdate(tx, futarchyActionsPackageId, action)
      .deleteSetProposalsEnabled(tx, config, expired)
      .deleteUpdateName(tx, config, expired)
      .deleteTradingParamsUpdate(tx, config, expired)
      .deleteMetadataUpdate(tx, config, expired)
      .deleteTwapConfigUpdate(tx, config, expired)
      .deleteGovernanceUpdate(tx, config, expired)
      .deleteMetadataTableUpdate(tx, config, expired)
      .deleteQueueParamsUpdate(tx, config, expired)
      .deleteSponsorshipConfigUpdate(tx, config, expired)
      .deleteConfigAction(tx, config, expired)
    .daoDissolution
      .createDissolutionCapabilityMarker(tx, futarchyActionsPackageId)
      .newCreateDissolutionCapability(tx, config)
      .doCreateDissolutionCapability(tx, config, executable, versionWitness, intentWitness)
    .daoDissolutionOps  // DAODissolutionOperations
    .daoQuota
      .setQuotasMarker(tx, futarchyActionsPackageId)
      .newSetQuotas(tx, config)
      .doSetQuotas(tx, config, executable, versionWitness, intentWitness)
      .getUsers(tx, futarchyActionsPackageId, action)
      .getQuotaAmount(tx, futarchyActionsPackageId, action)
      .getQuotaPeriodMs(tx, futarchyActionsPackageId, action)
      .getReducedFee(tx, futarchyActionsPackageId, action)
      .getSponsorQuotaAmount(tx, futarchyActionsPackageId, action)
      .deleteSetQuotas(tx, futarchyActionsPackageId, expired)
    .governanceIntents
      .witness(tx, governanceActionsPackageId)
      .executeProposalIntent(tx, config, outcome)
      .createAndStoreIntentFromSpec(tx, config, specs, outcome)
    .intentJanitor  // IntentJanitorOperations
    .packageRegistry
      .addPackageMarker(tx, governanceActionsPackageId)
      .removePackageMarker(tx, governanceActionsPackageId)
      .updatePackageVersionMarker(tx, governanceActionsPackageId)
      .updatePackageMetadataMarker(tx, governanceActionsPackageId)
      .pauseAccountCreationMarker(tx, governanceActionsPackageId)
      .unpauseAccountCreationMarker(tx, governanceActionsPackageId)
      .newAddPackage(tx, config)
      .newRemovePackage(tx, config)
      .newUpdatePackageVersion(tx, config)
      .newUpdatePackageMetadata(tx, config)
      .newPauseAccountCreation(tx, governanceActionsPackageId)
      .newUnpauseAccountCreation(tx, governanceActionsPackageId)
      .doAddPackage(tx, config, executable, versionWitness, intentWitness)
      .doRemovePackage(tx, config, executable, versionWitness, intentWitness)
      .doUpdatePackageVersion(tx, config, executable, versionWitness, intentWitness)
      .doUpdatePackageMetadata(tx, config, executable, versionWitness, intentWitness)
      .doPauseAccountCreation(tx, config, executable, versionWitness, intentWitness)
      .doUnpauseAccountCreation(tx, config, executable, versionWitness, intentWitness)
    .protocolAdmin
      .setFactoryPausedMarker(tx, governanceActionsPackageId)
      .disableFactoryPermanentlyMarker(tx, governanceActionsPackageId)
      .addStableTypeMarker(tx, governanceActionsPackageId)
      .removeStableTypeMarker(tx, governanceActionsPackageId)
      .updateDaoCreationFeeMarker(tx, governanceActionsPackageId)
      .updateProposalFeeMarker(tx, governanceActionsPackageId)
      .updateVerificationFeeMarker(tx, governanceActionsPackageId)
      .withdrawFeesToTreasuryMarker(tx, governanceActionsPackageId)
      .addVerificationLevelMarker(tx, governanceActionsPackageId)
      .removeVerificationLevelMarker(tx, governanceActionsPackageId)
      .addCoinFeeConfigMarker(tx, governanceActionsPackageId)
      .updateCoinCreationFeeMarker(tx, governanceActionsPackageId)
      .updateCoinProposalFeeMarker(tx, governanceActionsPackageId)
      .applyPendingCoinFeesMarker(tx, governanceActionsPackageId)
      .newSetFactoryPaused(tx, config)
      .newDisableFactoryPermanently(tx, governanceActionsPackageId)
      .newAddStableType(tx, config)
      .newRemoveStableType(tx, config)
      .newUpdateDaoCreationFee(tx, config)
      .newUpdateProposalFee(tx, config)
      .newUpdateVerificationFee(tx, config)
      .newAddVerificationLevel(tx, config)
      .newRemoveVerificationLevel(tx, config)
      .newWithdrawFeesToTreasury(tx, config)
      .newAddCoinFeeConfig(tx, config)
      .newUpdateCoinCreationFee(tx, config)
      .newUpdateCoinProposalFee(tx, config)
      .newApplyPendingCoinFees(tx, config)
      .doSetFactoryPaused(tx, config, executable, versionWitness, intentWitness)
      .doDisableFactoryPermanently(tx, config, executable, versionWitness, intentWitness)
      .doAddStableType(tx, config, executable, versionWitness, intentWitness)
      .doRemoveStableType(tx, config, executable, versionWitness, intentWitness)
      .doUpdateDaoCreationFee(tx, config, executable, versionWitness, intentWitness)
      .doUpdateProposalFee(tx, config, executable, versionWitness, intentWitness)
      .doUpdateVerificationFee(tx, config, executable, versionWitness, intentWitness)
      .doAddVerificationLevel(tx, config, executable, versionWitness, intentWitness)
      .doRemoveVerificationLevel(tx, config, executable, versionWitness, intentWitness)
      .doWithdrawFeesToTreasury(tx, config, executable, versionWitness, intentWitness)
      .doAddCoinFeeConfig(tx, config, executable, versionWitness, intentWitness)
      .doUpdateCoinCreationFee(tx, config, executable, versionWitness, intentWitness)
      .doUpdateCoinProposalFee(tx, config, executable, versionWitness, intentWitness)
      .doApplyPendingCoinFees(tx, config, executable, versionWitness, intentWitness)
    .oracle  // OracleActions

// Top-level methods
sdk.getRaises()
sdk.getDaos()
sdk.getProposals()
sdk.getPackageId(packageName)
sdk.getAllPackageIds()

// ============================================================================
// UTILITIES
// ============================================================================

sdk.utils
  .transactionBuilder  // BaseTransactionBuilder
    .getTransaction()
    .build()
    .splitSui(amount)
    .makeVector(items)
    .pureString(value)
    .pureU64(value)
    .pureU128(value)
    .pureBool(value)
    .pureAddress(address)
    .moveCall(args)
    .transferObjects(objects, recipient)
  .queryHelper  // QueryHelper
    .getObject(objectId)
    .getObjects(objectIds)
    .getOwnedObjects(address, filter)
    .getDynamicFields(parentObjectId)
    .getDynamicFieldObject(parentObjectId, name)
    .queryEvents(query)
    .extractField(object, fieldPath)
    .getBalance(address, coinType)
    .getAllBalances(address)
    .findTreasuryCap(coinType)
    .isObjectShared(objectId)
  .currency  // CurrencyUtils
    .getDecimals(coinType)
    .getMetadata(coinType)
    .getTotalSupply(coinType)
    .burn(config)
    .formatAmount(amount, decimals)
    .parseAmount(amountStr, decimals)
sdk.client
sdk.network
sdk.deployments
  .getPackage(packageName)
  .getPackageId(packageName)
  .getAllSharedObjects()
  .getAllAdminCaps()
  .getFactory()
  .getPackageRegistry()
  .getPackageAdminCap()
  .getFeeAdminCap()
  .getAllPackageIds()
sdk.packages
sdk.sharedObjects
```